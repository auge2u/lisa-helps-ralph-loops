# Logs
*.log
firebase-debug.log

# OS files
.DS_Store
Thumbs.db

# Editor directories
.idea/
.vscode/
*.swp
*.swo

# Output directories (generated by plugin at target project root)
# Note: scopecraft/ is tracked in THIS repo because lisa3 is the ecosystem root.
# It contains the architecture vision and reconcile outputs, not generated per-project plans.

# Local settings
.claude/settings.local.json

# Gastown runtime data (keep templates, ignore user data)
# Note: .gt/memory/*.json templates are tracked in this repo
# User-specific beads and convoys are generated per-project
